'use strict';

var jsxRuntime = require('react/jsx-runtime');
var React = require('react');
var designSystem = require('@strapi/design-system');
var icons = require('@strapi/icons');
var reactIntl = require('react-intl');
var styledComponents = require('styled-components');
var translations = require('../../../utils/translations.js');
var useListFilters = require('../hooks/useListFilters.js');
var DateRangeCalendar = require('./DateRangeCalendar.js');

const TYPE_LABELS = {
    folder: {
        id: translations.getTranslationKey('list.filters.type.folder'),
        defaultMessage: 'Folder'
    },
    picture: {
        id: translations.getTranslationKey('list.filters.type.picture'),
        defaultMessage: 'Picture'
    },
    audio: {
        id: translations.getTranslationKey('list.filters.type.audio'),
        defaultMessage: 'Audio'
    },
    video: {
        id: translations.getTranslationKey('list.filters.type.video'),
        defaultMessage: 'Video'
    },
    document: {
        id: translations.getTranslationKey('list.filters.type.document'),
        defaultMessage: 'Document'
    }
};
const PRESET_LABELS = {
    '1day': {
        id: translations.getTranslationKey('list.filters.preset.1day'),
        defaultMessage: '1 day ago'
    },
    '3days': {
        id: translations.getTranslationKey('list.filters.preset.3days'),
        defaultMessage: '3 days ago'
    },
    '1week': {
        id: translations.getTranslationKey('list.filters.preset.1week'),
        defaultMessage: '1 week ago'
    },
    '1month': {
        id: translations.getTranslationKey('list.filters.preset.1month'),
        defaultMessage: '1 month ago'
    },
    '3months': {
        id: translations.getTranslationKey('list.filters.preset.3months'),
        defaultMessage: '3 months ago'
    },
    '6months': {
        id: translations.getTranslationKey('list.filters.preset.6months'),
        defaultMessage: '6 months ago'
    },
    '1year': {
        id: translations.getTranslationKey('list.filters.preset.1year'),
        defaultMessage: '1 year ago'
    }
};
const DATE_FIELD_LABELS = {
    createdAt: {
        id: translations.getTranslationKey('list.filters.field.created'),
        defaultMessage: 'Creation date'
    },
    updatedAt: {
        id: translations.getTranslationKey('list.filters.field.updated'),
        defaultMessage: 'Last modified'
    }
};
// The DS SubTrigger renders its chevron inline right after the label — push it
// to the far edge so every field row reads label · · · ›, like the mock.
const FieldSubTrigger = styledComponents.styled(designSystem.Menu.SubTrigger)`
  width: 100%;
  justify-content: space-between;
`;
/** Every panel of the filter tree shares this fixed width (design). */ const FILTER_PANEL_WIDTH = '24.2rem';
// Sub panels: fixed width, no 15rem clamp (the DS default folds the calendar),
// and a negative top margin cancelling the parent panel's padding + border so
// the sub panel's top edge lines up with the parent panel, not the item.
const SubPanel = styledComponents.styled(designSystem.Menu.SubContent)`
  margin-top: calc(-1 * (${({ theme })=>theme.spaces[1]} + 1px));
`;
const CountBadge = styledComponents.styled(designSystem.Badge)`
  height: 1.6rem;
  min-width: auto;
  padding: 0 0.4rem;
`;
/**
 * Toolbar "Filter" dropdown. Picking anything ADDS a badge (editing happens on
 * the badge itself, see FilterBadges):
 *
 * - Type → keep-open checkbox list; the badge is created/updated live with the
 *   checked set (`is` by default, condition editable on the badge).
 * - Creation date / Last modified → preset list (single pick, `within the
 *   last` by default) + "Select date range" (creation date only) opening the
 *   range calendar; a completed range adds an `is` range badge.
 */ const FilterMenu = ({ listFilters })=>{
    const { formatMessage } = reactIntl.useIntl();
    const [isOpen, setIsOpen] = React.useState(false);
    const { filters, addFilter, updateFilter, removeFilter } = listFilters;
    // The Type submenu edits the LAST type badge in place while the menu stays
    // open (checking values accumulates into one badge, per design).
    let lastTypeIndex = -1;
    for(let i = filters.length - 1; i >= 0; i -= 1){
        if (filters[i].kind === 'type') {
            lastTypeIndex = i;
            break;
        }
    }
    const lastTypeFilter = lastTypeIndex >= 0 ? filters[lastTypeIndex] : null;
    const checkedValues = lastTypeFilter && lastTypeFilter.kind === 'type' ? lastTypeFilter.values : [];
    const toggleTypeValue = (value)=>{
        const nextValues = checkedValues.includes(value) ? checkedValues.filter((v)=>v !== value) : [
            ...checkedValues,
            value
        ];
        if (lastTypeFilter && lastTypeFilter.kind === 'type') {
            if (nextValues.length === 0) {
                // Unchecking the final value removes the badge outright — a type
                // filter with no values is not a state (it would serialize to a
                // malformed `type:is:` the parser drops anyway).
                removeFilter(lastTypeIndex);
            } else {
                updateFilter(lastTypeIndex, {
                    ...lastTypeFilter,
                    values: nextValues
                });
            }
        } else if (nextValues.length > 0) {
            addFilter({
                kind: 'type',
                condition: 'is',
                values: nextValues
            });
        }
    };
    const addPreset = (field, preset)=>{
        addFilter({
            kind: 'date',
            field,
            mode: 'preset',
            condition: 'withinLast',
            preset
        });
    };
    const addRange = (from, to)=>{
        addFilter({
            kind: 'date',
            field: 'createdAt',
            mode: 'range',
            condition: 'is',
            from,
            to
        });
        // The calendar is not a Menu.Item — close the menu explicitly on commit.
        setIsOpen(false);
    };
    return /*#__PURE__*/ jsxRuntime.jsxs(designSystem.Menu.Root, {
        open: isOpen,
        onOpenChange: setIsOpen,
        children: [
            /*#__PURE__*/ jsxRuntime.jsx(designSystem.Menu.Trigger, {
                variant: "tertiary",
                startIcon: /*#__PURE__*/ jsxRuntime.jsx(icons.Filter, {
                    "aria-hidden": true
                }),
                endIcon: null,
                children: /*#__PURE__*/ jsxRuntime.jsxs(designSystem.Flex, {
                    gap: 2,
                    alignItems: "center",
                    tag: "span",
                    children: [
                        formatMessage({
                            id: translations.getTranslationKey('list.filters.trigger'),
                            defaultMessage: 'Filter'
                        }),
                        filters.length > 0 && /*#__PURE__*/ jsxRuntime.jsx(CountBadge, {
                            children: filters.length
                        })
                    ]
                })
            }),
            /*#__PURE__*/ jsxRuntime.jsxs(designSystem.Menu.Content, {
                popoverPlacement: "bottom-start",
                zIndex: 2,
                maxHeight: "70vh",
                width: FILTER_PANEL_WIDTH,
                children: [
                    /*#__PURE__*/ jsxRuntime.jsxs(designSystem.Menu.SubRoot, {
                        children: [
                            /*#__PURE__*/ jsxRuntime.jsx(FieldSubTrigger, {
                                children: formatMessage({
                                    id: translations.getTranslationKey('list.filters.field.type'),
                                    defaultMessage: 'Type'
                                })
                            }),
                            /*#__PURE__*/ jsxRuntime.jsx(SubPanel, {
                                zIndex: 2,
                                maxHeight: "70vh",
                                width: FILTER_PANEL_WIDTH,
                                children: useListFilters.TYPE_VALUES.map((value)=>/*#__PURE__*/ jsxRuntime.jsx(designSystem.Menu.Item, {
                                        role: "menuitemcheckbox",
                                        "aria-checked": checkedValues.includes(value),
                                        onSelect: (e)=>{
                                            e.preventDefault();
                                            toggleTypeValue(value);
                                        },
                                        startIcon: /*#__PURE__*/ jsxRuntime.jsx(designSystem.Checkbox, {
                                            checked: checkedValues.includes(value),
                                            tabIndex: -1,
                                            "aria-hidden": true
                                        }),
                                        children: formatMessage(TYPE_LABELS[value])
                                    }, value))
                            })
                        ]
                    }),
                    [
                        'createdAt',
                        'updatedAt'
                    ].map((field)=>/*#__PURE__*/ jsxRuntime.jsxs(designSystem.Menu.SubRoot, {
                            children: [
                                /*#__PURE__*/ jsxRuntime.jsx(FieldSubTrigger, {
                                    children: formatMessage(DATE_FIELD_LABELS[field])
                                }),
                                /*#__PURE__*/ jsxRuntime.jsxs(SubPanel, {
                                    zIndex: 2,
                                    maxHeight: "70vh",
                                    width: FILTER_PANEL_WIDTH,
                                    children: [
                                        useListFilters.DATE_PRESETS.map((preset)=>/*#__PURE__*/ jsxRuntime.jsx(designSystem.Menu.Item, {
                                                onSelect: ()=>addPreset(field, preset),
                                                children: formatMessage(PRESET_LABELS[preset])
                                            }, preset)),
                                        field === 'createdAt' && /*#__PURE__*/ jsxRuntime.jsxs(designSystem.Menu.SubRoot, {
                                            children: [
                                                /*#__PURE__*/ jsxRuntime.jsx(FieldSubTrigger, {
                                                    children: formatMessage({
                                                        id: translations.getTranslationKey('list.filters.select-date-range'),
                                                        defaultMessage: 'Select date range'
                                                    })
                                                }),
                                                /*#__PURE__*/ jsxRuntime.jsx(SubPanel, {
                                                    zIndex: 2,
                                                    maxHeight: "none",
                                                    width: FILTER_PANEL_WIDTH,
                                                    children: /*#__PURE__*/ jsxRuntime.jsx(DateRangeCalendar.DateRangeCalendar, {
                                                        onSelect: addRange
                                                    })
                                                })
                                            ]
                                        })
                                    ]
                                })
                            ]
                        }, field))
                ]
            })
        ]
    });
};

exports.DATE_FIELD_LABELS = DATE_FIELD_LABELS;
exports.FILTER_PANEL_WIDTH = FILTER_PANEL_WIDTH;
exports.FilterMenu = FilterMenu;
exports.PRESET_LABELS = PRESET_LABELS;
exports.TYPE_LABELS = TYPE_LABELS;
//# sourceMappingURL=FilterMenu.js.map
