import { jsxs, jsx } from 'react/jsx-runtime';
import { useState } from 'react';
import { Menu, Flex, Checkbox, Badge } from '@strapi/design-system';
import { Filter } from '@strapi/icons';
import { useIntl } from 'react-intl';
import { styled } from 'styled-components';
import { getTranslationKey } from '../../../utils/translations.mjs';
import { TYPE_VALUES, DATE_PRESETS } from '../hooks/useListFilters.mjs';
import { DateRangeCalendar } from './DateRangeCalendar.mjs';

const TYPE_LABELS = {
    folder: {
        id: getTranslationKey('list.filters.type.folder'),
        defaultMessage: 'Folder'
    },
    picture: {
        id: getTranslationKey('list.filters.type.picture'),
        defaultMessage: 'Picture'
    },
    audio: {
        id: getTranslationKey('list.filters.type.audio'),
        defaultMessage: 'Audio'
    },
    video: {
        id: getTranslationKey('list.filters.type.video'),
        defaultMessage: 'Video'
    },
    document: {
        id: getTranslationKey('list.filters.type.document'),
        defaultMessage: 'Document'
    }
};
const PRESET_LABELS = {
    '1day': {
        id: getTranslationKey('list.filters.preset.1day'),
        defaultMessage: '1 day ago'
    },
    '3days': {
        id: getTranslationKey('list.filters.preset.3days'),
        defaultMessage: '3 days ago'
    },
    '1week': {
        id: getTranslationKey('list.filters.preset.1week'),
        defaultMessage: '1 week ago'
    },
    '1month': {
        id: getTranslationKey('list.filters.preset.1month'),
        defaultMessage: '1 month ago'
    },
    '3months': {
        id: getTranslationKey('list.filters.preset.3months'),
        defaultMessage: '3 months ago'
    },
    '6months': {
        id: getTranslationKey('list.filters.preset.6months'),
        defaultMessage: '6 months ago'
    },
    '1year': {
        id: getTranslationKey('list.filters.preset.1year'),
        defaultMessage: '1 year ago'
    }
};
const DATE_FIELD_LABELS = {
    createdAt: {
        id: getTranslationKey('list.filters.field.created'),
        defaultMessage: 'Creation date'
    },
    updatedAt: {
        id: getTranslationKey('list.filters.field.updated'),
        defaultMessage: 'Last modified'
    }
};
// The DS SubTrigger renders its chevron inline right after the label — push it
// to the far edge so every field row reads label · · · ›, like the mock.
const FieldSubTrigger = styled(Menu.SubTrigger)`
  width: 100%;
  justify-content: space-between;
`;
/** Every panel of the filter tree shares this fixed width (design). */ const FILTER_PANEL_WIDTH = '24.2rem';
// Sub panels: fixed width, no 15rem clamp (the DS default folds the calendar),
// and a negative top margin cancelling the parent panel's padding + border so
// the sub panel's top edge lines up with the parent panel, not the item.
const SubPanel = styled(Menu.SubContent)`
  margin-top: calc(-1 * (${({ theme })=>theme.spaces[1]} + 1px));
`;
const CountBadge = styled(Badge)`
  height: 1.6rem;
  min-width: auto;
  padding: 0 0.4rem;
`;
/**
 * Toolbar "Filter" dropdown. Picking anything ADDS a badge (editing happens on
 * the badge itself, see FilterBadges):
 *
 * - Type → keep-open checkbox list; the badge is created/updated live with the
 *   checked set (`is` by default, condition editable on the badge).
 * - Creation date / Last modified → preset list (single pick, `within the
 *   last` by default) + "Select date range" (creation date only) opening the
 *   range calendar; a completed range adds an `is` range badge.
 */ const FilterMenu = ({ listFilters })=>{
    const { formatMessage } = useIntl();
    const [isOpen, setIsOpen] = useState(false);
    const { filters, addFilter, updateFilter, removeFilter } = listFilters;
    // The Type submenu edits the LAST type badge in place while the menu stays
    // open (checking values accumulates into one badge, per design).
    let lastTypeIndex = -1;
    for(let i = filters.length - 1; i >= 0; i -= 1){
        if (filters[i].kind === 'type') {
            lastTypeIndex = i;
            break;
        }
    }
    const lastTypeFilter = lastTypeIndex >= 0 ? filters[lastTypeIndex] : null;
    const checkedValues = lastTypeFilter && lastTypeFilter.kind === 'type' ? lastTypeFilter.values : [];
    const toggleTypeValue = (value)=>{
        const nextValues = checkedValues.includes(value) ? checkedValues.filter((v)=>v !== value) : [
            ...checkedValues,
            value
        ];
        if (lastTypeFilter && lastTypeFilter.kind === 'type') {
            if (nextValues.length === 0) {
                // Unchecking the final value removes the badge outright — a type
                // filter with no values is not a state (it would serialize to a
                // malformed `type:is:` the parser drops anyway).
                removeFilter(lastTypeIndex);
            } else {
                updateFilter(lastTypeIndex, {
                    ...lastTypeFilter,
                    values: nextValues
                });
            }
        } else if (nextValues.length > 0) {
            addFilter({
                kind: 'type',
                condition: 'is',
                values: nextValues
            });
        }
    };
    const addPreset = (field, preset)=>{
        addFilter({
            kind: 'date',
            field,
            mode: 'preset',
            condition: 'withinLast',
            preset
        });
    };
    const addRange = (from, to)=>{
        addFilter({
            kind: 'date',
            field: 'createdAt',
            mode: 'range',
            condition: 'is',
            from,
            to
        });
        // The calendar is not a Menu.Item — close the menu explicitly on commit.
        setIsOpen(false);
    };
    return /*#__PURE__*/ jsxs(Menu.Root, {
        open: isOpen,
        onOpenChange: setIsOpen,
        children: [
            /*#__PURE__*/ jsx(Menu.Trigger, {
                variant: "tertiary",
                startIcon: /*#__PURE__*/ jsx(Filter, {
                    "aria-hidden": true
                }),
                endIcon: null,
                children: /*#__PURE__*/ jsxs(Flex, {
                    gap: 2,
                    alignItems: "center",
                    tag: "span",
                    children: [
                        formatMessage({
                            id: getTranslationKey('list.filters.trigger'),
                            defaultMessage: 'Filter'
                        }),
                        filters.length > 0 && /*#__PURE__*/ jsx(CountBadge, {
                            children: filters.length
                        })
                    ]
                })
            }),
            /*#__PURE__*/ jsxs(Menu.Content, {
                popoverPlacement: "bottom-start",
                zIndex: 2,
                maxHeight: "70vh",
                width: FILTER_PANEL_WIDTH,
                children: [
                    /*#__PURE__*/ jsxs(Menu.SubRoot, {
                        children: [
                            /*#__PURE__*/ jsx(FieldSubTrigger, {
                                children: formatMessage({
                                    id: getTranslationKey('list.filters.field.type'),
                                    defaultMessage: 'Type'
                                })
                            }),
                            /*#__PURE__*/ jsx(SubPanel, {
                                zIndex: 2,
                                maxHeight: "70vh",
                                width: FILTER_PANEL_WIDTH,
                                children: TYPE_VALUES.map((value)=>/*#__PURE__*/ jsx(Menu.Item, {
                                        role: "menuitemcheckbox",
                                        "aria-checked": checkedValues.includes(value),
                                        onSelect: (e)=>{
                                            e.preventDefault();
                                            toggleTypeValue(value);
                                        },
                                        startIcon: /*#__PURE__*/ jsx(Checkbox, {
                                            checked: checkedValues.includes(value),
                                            tabIndex: -1,
                                            "aria-hidden": true
                                        }),
                                        children: formatMessage(TYPE_LABELS[value])
                                    }, value))
                            })
                        ]
                    }),
                    [
                        'createdAt',
                        'updatedAt'
                    ].map((field)=>/*#__PURE__*/ jsxs(Menu.SubRoot, {
                            children: [
                                /*#__PURE__*/ jsx(FieldSubTrigger, {
                                    children: formatMessage(DATE_FIELD_LABELS[field])
                                }),
                                /*#__PURE__*/ jsxs(SubPanel, {
                                    zIndex: 2,
                                    maxHeight: "70vh",
                                    width: FILTER_PANEL_WIDTH,
                                    children: [
                                        DATE_PRESETS.map((preset)=>/*#__PURE__*/ jsx(Menu.Item, {
                                                onSelect: ()=>addPreset(field, preset),
                                                children: formatMessage(PRESET_LABELS[preset])
                                            }, preset)),
                                        field === 'createdAt' && /*#__PURE__*/ jsxs(Menu.SubRoot, {
                                            children: [
                                                /*#__PURE__*/ jsx(FieldSubTrigger, {
                                                    children: formatMessage({
                                                        id: getTranslationKey('list.filters.select-date-range'),
                                                        defaultMessage: 'Select date range'
                                                    })
                                                }),
                                                /*#__PURE__*/ jsx(SubPanel, {
                                                    zIndex: 2,
                                                    maxHeight: "none",
                                                    width: FILTER_PANEL_WIDTH,
                                                    children: /*#__PURE__*/ jsx(DateRangeCalendar, {
                                                        onSelect: addRange
                                                    })
                                                })
                                            ]
                                        })
                                    ]
                                })
                            ]
                        }, field))
                ]
            })
        ]
    });
};

export { DATE_FIELD_LABELS, FILTER_PANEL_WIDTH, FilterMenu, PRESET_LABELS, TYPE_LABELS };
//# sourceMappingURL=FilterMenu.mjs.map
